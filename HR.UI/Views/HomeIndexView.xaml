<UserControl x:Class="HR.UI.Views.HomeIndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HR.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Register" Width="100" Command="{Binding AccountRegisterNavigationCommand}"/>
            <Button Content="Login" Width="100" Command="{Binding AccountLoginNavigationCommand}"/>
            <Button Content="" Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>
